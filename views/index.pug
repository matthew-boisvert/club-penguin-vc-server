doctype html
head
	title PenguinChat Server
	meta(property='og:title' content='PenguinChat Server')
	meta(property='og:description' content='Voice server for Club Penguin proximity chat')
	style.
		body {
		background-color: #23272a;
		color: white;
		font-family: sans-serif;
		text-align: center;
		}
		img {
		height: 128px;
		width: 128px;
		margin: 32px auto;
		display: block;
		}
		a {
			text-decoration: none;
			color: #3498db
		}
		a:hover {
			text-decoration: underline;
		}
h1 PenguinChat Server
p This is a PenguinChat Server running on #{address}.
p There #{connectionCount === 1 ? 'is' : 'are'} currently #{connectionCount} connected user#{connectionCount === 1 ? '' : 's'}.